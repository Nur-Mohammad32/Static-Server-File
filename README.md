# Node Static File Server

A minimal static file server built using Node.js core modules (`http`, `fs`, `path`).  
It serves HTML, CSS, and PNG files without using Express or external libraries.

 # Features

- Serves static files
- Basic MIME type handling
- 404 error handling
- Built with pure Node.js


```bash
node server.js
